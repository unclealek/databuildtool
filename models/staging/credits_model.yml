
version: 2

models:
  - name: credits_model
    description: "Staging model for movie credit records."
    columns:
      - name: movie_id
        description: "Unique identifier for the movie."
        data_tests:
          - unique
          - not_null

